import React from 'react'
import { connect } from 'react-redux' 

const Runs = () => {
    return(
        <div>
            here is where all my hardwork will sit
        </div>
    )
}

export default connect(mapStateToProps)(Runs)